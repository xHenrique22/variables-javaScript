/*
 Crie um programa de saúde que trabalhe com variáveis que recebam dados de peso,
 nome, altura, data de internação e uma variável que receba o dado se o paciente é diabético ou não.
*/

const prompt = require('prompt-sync')();

const peso = prompt('Qual o seu peso: ')
const nome = prompt('Qual seu nome: ')
const altura = prompt('Qual a sua altura: ')
const data = prompt('Data de internação: ')
const diabetico = prompt('Diabético - Digite 1 para sim e 2 para não: ')


